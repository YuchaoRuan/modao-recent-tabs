(function () { var VERSION_EXPECTED = "1.0.20"; var KEY_RE = /搜索|查找|检索/; var qa = function (s) { try { return document.querySelectorAll(s); } catch (e) { return []; } }; var brief = function (n) { var c = String(n.className || "").trim().split(/\s+/).filter(Boolean).slice(0, 4).join("."); return n.tagName.toLowerCase() + (c ? "." + c : "") + (n.id ? "#" + n.id : ""); }; var chainOf = function (el, max) { var o = [], n = el, i = 0; while (n && n.nodeType === 1 && i++ < (max || 6)) { o.push(brief(n)); n = n.parentElement; } return o; }; var rectOf = function (el) { var r = { top: 0, left: 0, width: 0, height: 0 }; try { var b = el.getBoundingClientRect(); r = { top: Math.round(b.top), left: Math.round(b.left), width: Math.round(b.width), height: Math.round(b.height) }; } catch (e) {} return r; }; var fieldsOf = function (el) { var r = rectOf(el); var tag = el.tagName.toLowerCase(); var tp = (tag === "input") ? el.type : (el.getAttribute("role") || ""); var ph = el.getAttribute ? (el.getAttribute("placeholder") || "") : ""; return { tag: tag, type: tp, placeholder: ph, width: r.width, height: r.height, top: r.top, left: r.left, visible: (r.width > 0 && r.height > 0), ancestorChain: chainOf(el, 5), keyHitField: null }; }; var keyHitFieldOf = function (el) { var fields = [ ["placeholder", el.getAttribute ? (el.getAttribute("placeholder") || "") : ""], ["value", (typeof el.value === "string") ? el.value : ""], ["aria-label", el.getAttribute ? (el.getAttribute("aria-label") || "") : ""], ["name", el.getAttribute ? (el.getAttribute("name") || "") : ""], ["id", el.id || ""], ["className", String(el.className || "")] ]; for (var k = 0; k < fields.length; k++) { if (KEY_RE.test(fields[k][1])) { return fields[k][0]; } } return null; }; var rootEl = document.querySelector("#md-recent-tabs-root"); var ver = rootEl ? rootEl.getAttribute("data-md-version") : null; var allInputs = qa("input"); var total = allInputs.length; var searchLike = []; var placeholderTop300 = []; for (var i = 0; i < allInputs.length; i++) { var el = allInputs[i]; var r = rectOf(el); var ph = el.getAttribute ? (el.getAttribute("placeholder") || "") : ""; if (r.top < 300) { placeholderTop300.push(ph); } var hit = keyHitFieldOf(el); if (hit) { var so = fieldsOf(el); so.keyHitField = hit; searchLike.push(so); } } var topInputs = []; for (var j = 0; j < allInputs.length && j < 15; j++) { var to = fieldsOf(allInputs[j]); to.keyHitField = keyHitFieldOf(allInputs[j]); topInputs.push(to); } var ces = qa('[contenteditable="true"]'); var contentEditables = []; for (var c = 0; c < ces.length && c < 10; c++) { contentEditables.push(fieldsOf(ces[c])); } var candEls = qa('input, [contenteditable], [role="searchbox"], [role="textbox"]'); var allCandidateBoxes = []; for (var m = 0; m < candEls.length && allCandidateBoxes.length < 200; m++) { var ce = candEls[m]; var cr = rectOf(ce); if (cr.top < 300 && cr.width > 40) { allCandidateBoxes.push(fieldsOf(ce)); } } var screenList = document.querySelector("#screen-scroll-list"); var leftPanelProbe = null; if (screenList) { var chain = []; var n = screenList, idx = 0; while (n && n.nodeType === 1 && idx < 6) { var nr = rectOf(n); var cs = null; try { cs = window.getComputedStyle(n); } catch (e) {} chain.push({ node: brief(n), overflowY: cs ? (cs.overflowY || "") : "", top: nr.top, height: nr.height }); n = n.parentElement; idx++; } leftPanelProbe = chain; } var out = { ts: new Date().toISOString(), url: location.href, coreVersion: ver, expectedVersion: VERSION_EXPECTED, versionMatchesExpected: ver === VERSION_EXPECTED, total: total, searchLike: searchLike, placeholderTop300: placeholderTop300, topInputs: topInputs, contentEditables: contentEditables, allCandidateBoxes: allCandidateBoxes, leftPanelProbe: leftPanelProbe }; var s = JSON.stringify(out, null, 1); console.log(s); if (typeof copy === "function") { copy(s); } return out; })()